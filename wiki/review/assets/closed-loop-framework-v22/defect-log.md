# 检查记录

## 静态预检
- log_a穿过辅助信号注释：P0，移至外侧；首张预览前清除。
- 跨面板纵向间隔警告：已检查。不同语义带需要不同间距，不是同类模块不对齐；同排模块间距一致，允许。

## Cycle 1：1700px整图
| 区域/元素 | 发现 | 等级 | 处理 |
|---|---|---|---|
| 文本 a0 | “因果处理／质量标记”贴近边界，字体实测超过内部宽度 | P1 | 改为因果处理与质控 |
| 箭头 a_adapt | 返回竖线与安全反馈横线交叉 | P1 | 交换返回端口，形成嵌套路径 |
| 日志 log_e | 标签超出右画布 | P0 | 改为共享日志层 |
| 语义 日志 | 只有首尾模块关联，其他评价字段缺乏明确来源关系 | P1 | 五模块共同关联独立记录层 |
| 文本 optional | 可选模态与日志关系争用空间 | P1 | 移到左上输入区域 |
| 构图 面板标题 | 两面板标题中心不一致 | P2 | 统一宽度 |
| 语义 b4 | 无风险条件缺少回到下一轮判断的路径 | P1 | 增加顶侧返回 |
| 评价边界 | 长期结局不应理解为运行日志直接提供 | P1 | 明确结合独立结局测量 |

九区检查：文本、箭头、盒体、间距、颜色、字体、构图、图标和整体一致性均检查。未使用图标，无相关缺陷。不为满足数量而虚构30项问题。

导出限制：本地受限XML渲染器并非draw.io原生渲染，最终需要在draw.io打开复核。XML静态结构与派生PDF可独立检查。

## Cycle 2：完整预览复核
- 辅助信号容器高度预检警告：由65改80，消除。
- “无风险”措辞过强：改为“未触发暂停”。
- 安全反馈与主流程仅颜色不同：改成长虚线；自适应改短虚线，日志保留点线，更新图例。
- 恢复路径经共享重新判断节点返回准入，修改标签避免暗示直接连入准入框。

## Cycle 3：最终PNG与17 cm论文宽度
- 通过XeLaTeX将矢量PDF以170 mm嵌入A4页面，并渲染检查；正文未修改。
- 最小字号26画布单位对应7.37 pt，模块字号约7.94 pt；未发现裁切、穿框或标签重叠。
- XML结构检查：25个文本/模块对象、27条边，无内嵌位图、重复ID、占位标签。
- 视觉预检：0 fail，1 warn。跨语义区纵向间距不一致属于有意分区，保留。
- PDF宽170 mm，PNG为300 dpi；PDF文字与路径为矢量。

## 对抗性语义复核
1. EEG明确为主信号。
2. 辅助模态注明可选。
3. 阶段/事件/相位按任务与范式选择。
4. 物理输出对象为受试者，而非记忆或临床终点。
5. 响应来自持续信号采集。
6. 安全返回终点是决策模块。
7. 自适应标明可选且需系统级验证。
8. 独立评价层不作为实时控制箭头。
9. 三项检查失败均有拒绝/等待路径。
10. 图注明作者综合框架，不冒充单一既有算法。

自评：语义9/10、可读性8/10、布局8/10、编辑性8/10、验证7/10，合计40/50。扣分主要为信息密度与未完成draw.io原生渲染验证。不声称已完成发表级验收。
